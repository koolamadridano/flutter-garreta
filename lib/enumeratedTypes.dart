enum Location {
  adminArea,
  featureName,
  locality,
  subLocality,
  thoroughfare,
  addressLine,
  featureNameAndSubLocality,
  featureNameAndLocality,
}

enum Qty { add, minus }

enum ProductScreenType {
  grid,
  list,
}
